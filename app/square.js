function Square (bonus)
{
  this.letter = null;  // each square starts with no letter on it
  this.bonus = bonus;  // "st", "dl", "tl", "dw", "tw", ".."
                       // ".." is a blank square
}
